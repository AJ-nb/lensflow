# Lensflow 隐私说明

最后更新：2026-08-29

Lensflow 不运营账号、分析埋点或云数据库，也不会把用户图片、提示词、API Key 或本地资产发送给项目维护者。

## 本地数据

- API Key 默认保存在 Chrome 会话存储，用户明确选择后才持久化到扩展本地存储。
- 捕捉、分析、提示词、参考关系、任务、资产、收藏、历史和非敏感设置保存在本机 IndexedDB。
- 备份和脱敏诊断包永不包含 API Key、Authorization 头或 Provider secret。
- 网站 Studio 通过版本化 MessageChannel 桥接使用同一份插件数据，不形成云端或跨设备同步。

## Provider 请求

只有用户主动执行模型发现、能力探测、分析、生成或编辑时，插件才连接用户选择的 Provider。请求可能包含界面预检列出的提示词、模型、图片和用户确认的参考内容。Provider 的保留政策由用户自行核验。

超时、断网、429、5xx 或 Schema 失败不会自动重发付费生成。失败位置只能由用户手动补全。

## 权限与本机服务

- `activeTab` 与 `scripting` 仅用于用户打开工作流时在当前网页提供图片选择。
- Lensflow 正式网站桥接仅匹配 `https://aj-nb.github.io/lensflow/*`。
- Provider、远程图片与 ComfyUI Origin 通过用户操作按需授权。
- Eagle 只在用户主动导出时访问 `http://localhost:41595`，并回读图库数量、文件夹、标签和元数据。

网页桥接只允许握手、版本、资产、任务、下载和打开备份界面；它不能读取密钥、浏览历史、任意文件或任意 URL。

## 删除与迁移

用户可以删除单项资产和历史、设置保留策略、导出备份，或在 Chrome 中清除扩展数据。不同扩展 ID 之间必须通过旧插件导出、新插件导入迁移；导入文件中的 API Key 会被无条件丢弃。
