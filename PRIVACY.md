# 隐私说明

最后更新：2026-08-13

砚台本身不运营服务器，也不会将用户图片、提示词、API Key 或分析档案发送给项目维护者。

## 发送到用户配置 API 的数据

用户主动运行 AI 概览、完整分析、图片编辑、复现或三视图时，砚台会向用户填写的 API Base URL 发送：

- 用户选择的图片或处理后的图片数据；
- 用户输入的补充提示词或编辑要求；
- 为获得结构化分析所需的系统提示词；
- 用户选择的模型名称和请求参数。

数据处理和保留方式由该 API 服务商决定。用户应在使用前阅读对应服务商的隐私政策。

## 本地数据

- 设置保存在 Chrome 扩展存储中。
- API Key 默认只保存在会话存储；用户可主动开启本地长期保存。
- 分析档案、收藏、标签、提示词版本与 Eagle 状态保存在浏览器 IndexedDB。
- OCR、相似图、调色和主体分区在本地执行。
- 默认备份不包含 API Key。

## 权限

- `storage`：保存设置、会话密钥与当前任务状态。
- `sidePanel`：提供主要界面。
- `contextMenus`：从图片右键菜单创建分析来源。
- `scripting`：在侧边栏打开期间向当前普通网页注入图片选择层。
- `http://*/*` 与 `https://*/*`：让侧边栏打开后无需重复授权即可在当前普通网页选图，也用于用户配置的 API、图片 URL 与本机 Eagle 连接。

网页选择层不会在浏览器启动时常驻，也不会自动读取或发送网页图片。只有用户在图片上点击“砚台分析”后，该图片才进入侧边栏；AI 请求仍需用户单独点击生成或编辑操作。

## 删除数据

用户可在设置中导出备份，在档案页删除单项记录，或通过 Chrome 扩展管理页面清除扩展数据。卸载扩展通常会删除该扩展 ID 对应的本地数据；建议卸载前导出备份。
