# 贡献指南

1. Fork 仓库并创建独立分支。
2. 不提交密钥、备份、个人图片、构建产物或 Eagle 图库数据。
3. 运行 `npm ci`、`npm run typecheck`、`npm test` 和 `npm run build`。
4. PR 描述需说明用户影响、权限变化、数据迁移和验证结果。
5. 新增第三方代码、模型或素材时，必须补充来源与许可说明。

涉及 API Key、权限、存储迁移、备份、远程图片和 Eagle 写入的修改必须包含回归测试。
