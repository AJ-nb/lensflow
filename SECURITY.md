# 安全政策

## 报告漏洞

请通过 GitHub 仓库的 Security Advisories 私下报告漏洞。不要在公开 Issue 中提交 API Key、访问令牌、个人图片、完整备份或可利用细节。

报告应包含受影响版本、复现步骤、预期与实际行为，以及不含敏感数据的最小证据。

## 支持范围

仅维护最新发布版本。依赖漏洞通过 `npm audit`、Dependabot 和 GitHub Actions 跟踪。

## 密钥处理

- 不要在源码、Issue、PR、截图或备份示例中提交真实密钥。
- 默认使用会话密钥模式。
- 若设备多人共用，不要开启长期保存。
- 一旦密钥疑似泄露，立即在 API 服务商后台撤销并重新生成。

## 威胁边界

浏览器扩展中的 API Key 不能像服务器密钥一样做到绝对隐藏。长期和多人使用时，最稳妥的方案是部署由用户控制的后端代理，并为每位用户设置独立身份、配额和审计。
